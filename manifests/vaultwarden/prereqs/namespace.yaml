---
# sync wave 0 — namespace must exist before all other prereqs resources.
apiVersion: v1
kind: Namespace
metadata:
  name: vaultwarden
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/name: vaultwarden
    argocd.argoproj.io/instance: vaultwarden-prereqs
