apiversion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: nodered
    user:${INFLUXDB_USER}
    password:${INFLUXDB_PASSWORD}
    url:${INFLUXDB_URL}
    isDefault: true
    editable: true
