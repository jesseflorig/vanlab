# Data Model: Vaultwarden Password Vault (066)

This spec has no application data model — it is an infrastructure deployment. This file catalogs the Kubernetes resource inventory instead.

## Kubernetes Resources

### Namespace

| Resource | Name | Namespace | Notes |
|---|---|---|---|
| Namespace | `vaultwarden` | — | ArgoCD sync wave 0 |

### Prereqs (ArgoCD Application: `vaultwarden-prereqs`)

| Resource | Name | Namespace | Sync Wave | Notes |
|---|---|---|---|---|
| Namespace | `vaultwarden` | — | 0 | |
| SealedSecret | `vaultwarden-admin-token` | `vaultwarden` | 1 | Generated by `seal-secrets.yml`; key: `admin-token` |
| IngressRoute | `vaultwarden` | `vaultwarden` | 2 | `vault.fleet1.lan`, websecure, standard TLS (no device-mTLS) |

### App (ArgoCD Application: `vaultwarden`)

| Resource | Kind | Name | Namespace | Notes |
|---|---|---|---|---|
| ArgoCD Application | Application | `vaultwarden` | `argocd` | Multi-source: guerzon/vaultwarden chart + this repo values |
| Deployment | Deployment | `vaultwarden` | `vaultwarden` | 1 replica, arm64 image pinned |
| PersistentVolumeClaim | PVC | `vaultwarden-data` | `vaultwarden` | 1Gi, `longhorn` StorageClass |
| Service | Service | `vaultwarden` | `vaultwarden` | ClusterIP, port 80 |

## External State Changes

| System | Change | How |
|---|---|---|
| OPNsense unbound | Add `vault.fleet1.lan` → Traefik node IP | Add `- hostname: vault` to `fleet1_lan_traefik_dns_records` in `network-deploy.yml`; re-run playbook |
| `group_vars/all.yml` | Add `vaultwarden_admin_token` | Manual edit (untracked file) |
| `group_vars/example.all.yml` | Add `vaultwarden_admin_token: CHANGEME` | Committed placeholder |
| `seal-secrets.yml` | Add vaultwarden task block | Extend playbook; generates `manifests/vaultwarden/prereqs/sealed-secrets.yaml` |

## Helm Values Summary

File: `manifests/vaultwarden/vaultwarden-values.yaml`

| Key | Value | Notes |
|---|---|---|
| `image.tag` | `<pinned-version>` | Determine from GitHub releases at implementation time |
| `signupsAllowed` | `true` → then `false` | `true` only during initial admin account creation; set `false` immediately after |
| `adminToken.existingSecret` | `vaultwarden-admin-token` | Matches SealedSecret name |
| `adminToken.existingSecretKey` | `admin-token` | Matches SealedSecret key |
| `storage.data.name` | `vaultwarden-data` | PVC name |
| `storage.data.size` | `1Gi` | Sufficient for single-user text vault |
| `storage.data.storageClass` | `longhorn` | Constitution Principle VIII |
| `ingress.enabled` | `false` | We use Traefik IngressRoute directly |

> Verify all key names against `helm show values vaultwarden/vaultwarden --version <pinned>` before committing values file.
